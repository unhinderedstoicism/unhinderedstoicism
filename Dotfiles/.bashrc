# .bashrc, used on interactive non-login shells (such as when you start a terminal emulator)

# source .bash_profile
source ~/.bash_profile

# starting commands
neofetch --disable "Packages" #run neofetch when starting a terminal, but do not show the package count as doing so is slow