# .bash_profile, used on the login shell

# source global definitions
source /etc/bashrc

# my aliases and functions
alias ls="ls -lA --color=auto" #have ls always show details. also show all files except for relative dirs. also use color when possible.